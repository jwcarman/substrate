LAST_RUN: 2026-04-10T19:20:00Z
ITERATION: 22
SPEC_COMPLETED: 021-mailbox-single-delivery.md
WHAT_WAS_DONE: Enforced single-delivery on Mailbox. Added MailboxFullException in substrate-api. Updated InMemoryMailboxSpi to throw MailboxFullException on re-delivery atomically inside ConcurrentHashMap.compute. Updated all 6 backend MailboxSpi implementations (Redis via Lua script, PostgreSQL via UPDATE...WHERE value IS NULL with follow-up existence check, Hazelcast via CAS replace(key, CREATED_MARKER, value), NATS via revision-checked update, MongoDB via value exists(false) filter, DynamoDB via ConditionExpression attribute_not_exists). Updated DefaultMailbox to propagate MailboxFullException. Added unit tests and concurrent-delivery tests (8 virtual threads) for InMemoryMailboxSpi, DefaultMailbox, and all 6 backend ITs. Fixed pre-existing issues: PostgreSQL schema missing expires_at column and nullable value (from spec 019), NATS empty byte array being stored as null (changed CREATED_MARKER to {0} sentinel).
VERIFICATION: ./mvnw spotless:check — PASS; ./mvnw verify — all mailbox tests pass (unit + IT across all 6 backends, 8 tests each); RedisJournalSpiIT has pre-existing failures unrelated to this spec
STATUS: MORE_WORK
NEXT_SPEC: 022-sweep-spi-and-core-sweepers.md
NOTES: Fixed two pre-existing bugs discovered during IT runs: (1) PostgreSQL V1__create_mailbox.sql was missing expires_at column and had value as NOT NULL — updated schema to match code expectations from spec 019. (2) NATS KV returns null for empty byte[] values — changed CREATED_MARKER from new byte[0] to new byte[]{0} and updated get() to compare with Arrays.equals. RedisJournalSpiIT failures are pre-existing and unrelated — all 7 tests in that class fail with connection/setup errors.
