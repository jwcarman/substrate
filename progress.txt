LAST_RUN: 2026-04-10T18:15:00Z
ITERATION: 20
SPEC_COMPLETED: 019-intentionally-leased-cleanup.md
WHAT_WAS_DONE: Implemented the intentionally-leased API cleanup and package migration — moved all user-facing interfaces (Journal, JournalFactory, JournalCursor, JournalEntry, Mailbox, MailboxFactory, MailboxExpiredException) to substrate-api under org.jwcarman.substrate.journal/mailbox packages; moved all implementations, SPIs, and in-memory fallbacks to substrate-core under org.jwcarman.substrate.core.* packages; renamed Notifier to NotifierSpi; removed Journal.append(T) no-TTL overload from the interface, SPI, and all 8 backend implementations; added creation-time TTL to MailboxFactory and MailboxSpi (new create(key, ttl) method + 3-state get semantics with MailboxExpiredException); implemented TTL enforcement in InMemoryJournalSpi and InMemoryMailboxSpi; added max-TTL-per-primitive enforcement (SubstrateProperties with atom=24h, journal=7d, mailbox=30m defaults) in DefaultAtomFactory, DefaultAtom, DefaultJournal, and DefaultMailboxFactory; converted JournalFactory and MailboxFactory from concrete classes to interfaces with DefaultJournalFactory/DefaultMailboxFactory implementations; updated all 20 backend modules for new imports and API changes; updated SubstrateAutoConfiguration and AutoConfiguration.imports; updated PRD.md to remove @ConditionalOnClass-only constraint, add Atom primitives section, and reflect substrate-api + substrate-core module split.
VERIFICATION: ./mvnw clean verify -DskipITs — BUILD SUCCESS (all 24 modules pass, 147+ tests in substrate-core all green); ./mvnw spotless:check — PASS
STATUS: MORE_WORK
NEXT_SPEC: 020-backend-module-consolidation.md
NOTES: All package moves are complete. The old packages (org.jwcarman.substrate.spi, org.jwcarman.substrate.memory, org.jwcarman.substrate.autoconfigure, and the old org.jwcarman.substrate.core flat package for Journal/Mailbox types) are fully empty and deleted. Backend modules retain their own package namespaces (e.g., org.jwcarman.substrate.journal.redis) — spec 020 handles the backend-side consolidation. Integration tests (ITs) were skipped in verification since they require running containers; all unit tests pass. The Mailbox SPI contract is now: create → deliver → get, with MailboxExpiredException for dead/expired mailboxes. Backend mailbox implementations were updated to support the new 3-state get semantics (throws for dead, empty for alive-no-value, present for delivered).
