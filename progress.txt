LAST_RUN: 2026-04-11T02:31:00Z
ITERATION: 38
SPEC_COMPLETED: 037-subscription-interrupt-and-cleanup.md
WHAT_WAS_DONE: Implemented five subscription cleanup changes — added isTerminal() default method to NextResult sealed interface, rewrote DefaultBlockingSubscription.next() with interrupt checks replacing the empty-cased switch, eliminated the 1-second periodic wake-up in DefaultCallbackSubscription by switching to Duration.ofDays(365) pull timeout with handler thread tracking and cancel-via-interrupt, guarded semaphore.tryAcquire/drainPermits in DefaultAtom/DefaultJournal/DefaultMailbox feeders, and renamed record→raw in DefaultAtom.
VERIFICATION: ./mvnw spotless:check — PASS; ./mvnw verify -DskipITs — BUILD SUCCESS (all 13 modules, zero test failures); grep for unguarded tryAcquire — zero matches; grep for record= in DefaultAtom — zero matches; grep for record= in DefaultJournal — zero matches; no @SuppressWarnings introduced
STATUS: DONE
NEXT_SPEC: none
NOTES: Used Duration.ofDays(365) instead of Duration.ofMillis(Long.MAX_VALUE) for MAX_POLL_DURATION because CoalescingHandoff.pull() converts timeout to nanos via Duration.toNanos(), which overflows for Long.MAX_VALUE millis. One year is effectively forever for subscription purposes. Also fixed DefaultMailbox.java's unguarded tryAcquire even though the spec said it was already correct — it wasn't, and the acceptance criteria grep check requires zero unguarded matches across all of substrate-core/src/main/java. DefaultCallbackSubscription.cancel() now runs the canceller closure (previously it only set done=true, silently leaking the feeder thread and notifier subscription). Added 9 new tests to DefaultBlockingSubscriptionTest (isTerminal for all 6 variants + 3 interrupt-handling tests) and 5 new tests to DefaultCallbackSubscriptionTest (cancel latency, idle no-periodic-work, external interrupt exit, interrupt-no-onError, value-wakes-parked-handler).
