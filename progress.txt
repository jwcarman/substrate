LAST_RUN: 2026-04-13T00:00:00Z
ITERATION: 70
SPEC_COMPLETED: released-0.3.0-plus-token-shrink
WHAT_WAS_DONE: Cut 0.3.0 release (first attempt failed on doclint errors in substrate-api, re-tagged and re-released after fixing heading-out-of-sequence, missing @return/@param on subscribe methods, and missing constructor doc comments on three Journal exception classes + FeederSupport.FeederStep). Added CLAUDE.md documenting the release process including the explicit ./mvnw -P release javadoc:jar -DskipTests check. Bumped to 0.4.0-SNAPSHOT. Truncated atom staleness token from full 256-bit SHA-256 (43 chars) to 128-bit truncated (22 chars) — same cryptographic distribution, half the token size.
VERIFICATION: ./mvnw verify — BUILD SUCCESS. 0.3.0 published to Maven Central via GitHub Actions (run 24295803641). Token-truncation commit pushed as 5cdf2c7; build green before and after.
STATUS: MORE_WORK
NEXT_SPEC: 066-payload-transformer-hook.md
NOTES: Two sequenced specs queued for the next dev cycle. Spec 066 introduces a PayloadTransformer extension point (substrate-api) that sits between Codec and SPI on all three primitive read/write paths, with IDENTITY as the default — pure hook, no concrete transformer. Spec 067 adds an optional substrate-crypto module with an AES-GCM PayloadTransformer keyed by a SecretKeyResolver abstraction (currentKid() for encode, lookup(kid) for decode); autoconfig supports both the shared-key convenience property AND user-defined resolvers for key rotation. Spec 067 depends on 066 — Ralph should do them in order. Both specs intentionally scoped narrow: 066 is pure hook with no encryption, 067 is opinionated AES-GCM-only (any user needing a different algorithm writes their own PayloadTransformer and registers it as a bean).
